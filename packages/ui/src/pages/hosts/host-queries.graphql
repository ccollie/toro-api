query HostsPageData {
  hosts {
    id
    name
    redis {
      ...RedisStats
    }
    jobCounts {
      active
      failed
      paused
      completed
      delayed
      waiting
    }
    queueCount
    workerCount
  }
}
