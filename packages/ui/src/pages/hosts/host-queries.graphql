query HostsPageData($range: String!, $granularity: MetricGranularity! = Minute) {
  hosts {
    id
    name
    redis {
      ...RedisStats
    }
    jobCounts {
      active
      failed
      paused
      completed
      delayed
      waiting
    }
    queueCount
    workerCount
  }
}
