import AbstractHistogramIterator from 'hdr-histogram-js/AbstractHistogramIterator';
import { createHistogram, computeHistogramBins } from '../../../src/server/stats';


describe('computeHistogramBins', () => {

    it('works', () => {
        let histogram = createHistogram();

        for (let i = 0; i < 10; i++) {

        }

    });
})
